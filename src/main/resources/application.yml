spring:
  datasource:
    url: "jdbc:mysql://localhost/networker"
    username: "root"
    password: "1631"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: "gmail 이름 ex) asd@gmail.com 이면 asd만 작성" "imap 설정 필요함"
    password: "앱 비밀번호 / gmail 2차 인증 생성후 앱 비밀번호 생성 해야 함"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

